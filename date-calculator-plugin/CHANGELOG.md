# Changelog

## 0.1.0 - 2026-07-28

- Added the date calculator as a page action plugin.
- Added `today`, `today + Nd`, and `today - Nd` expression handling.
- Moved action logic into `main.js`.
- Moved Internal Page layout into `page.js`.
- Added playground examples for common date expressions.
- Added `manifest.i18n` metadata.
- Added plugin-scoped `styles.css`.
